apiVersion: v1
kind: Service
metadata:
  ## Define headless service name, used to create DNS records for each pod
  name: simm-data-svc
  {{ if .Values.namespace }}
  namespace: {{ .Values.namespace }}
  {{ end }}
  labels:
    {{ if .Values.namespace }}
    app: app-{{ .Values.namespace }}-simm-data-svc
    {{ else }}
    app: app-simm-data-svc
    {{ end }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.simm.dataserverPort }}
      name: simm-dataserver-port
  selector:
    {{ if .Values.namespace }}
    app: app-{{ .Values.namespace }}-simm-data-svc
    {{ else }}
    app: app-simm-data-svc
    {{ end }}
---
apiVersion: apps/v1
kind: StatefulSet # Resource type
metadata: # Resource metadata (name, namespace, labels, etc.)
  name: simm-data-svc # Resource name, must be unique within the namespace
  {{ if .Values.namespace }}
  namespace: {{ .Values.namespace }}
  {{ end }}
spec: # Resource spec
  # serviceName references the headless service above, binding this StatefulSet to it
  serviceName: simm-data-svc
  replicas: {{ .Values.replicaCount }}
  selector: # Label selector
    matchLabels:
      {{ if .Values.namespace }}
      app: app-{{ .Values.namespace }}-simm-data-svc
      {{ else }}
      app: app-simm-data-svc
      {{ end }}
  podManagementPolicy: Parallel
  template: # Pod template
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{ if .Values.namespace }}
        app: app-{{ .Values.namespace }}-simm-data-svc
        {{ else }}
        app: app-simm-data-svc
        {{ end }}
    spec:
      restartPolicy: Always
      # hostNetwork: true
      # dnsPolicy: ClusterFirstWithHostNet
      hostIPC: true
      terminationGracePeriodSeconds: 10
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: simm-data-server
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.simm.dataserverPort }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          env:
          - name: POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          - name: SIMM_MEMORY_LIMIT
            valueFrom:
              resourceFieldRef:
                containerName: simm-data-server
                resource: limits.memory
          - name: SIMM_MEMORY_RESERVED
            value: "0"
          - name: APISERVER
            value: https://kubernetes.default.svc
          - name: SERVICEACCOUNT
            value: /var/run/secrets/kubernetes.io/serviceaccount
          - name: NODE_NAME
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
          - name: SICL_LOG_LEVEL
            value: WARN
          - name: SICL_LOG_FILE
            value: /var/log/sicl/sicl-transport-ds.log
          - name: SICL_CHANNEL_SWEEP
            value: "1"
          {{ if .Values.dataserver_debug }}
          command: ["/bin/bash", "-c", "sysctl -w kernel.core_pattern=\"|/usr/bin/lz4 - /var/lib/systemd/coredump/core.%E.%h.lz4 -z -f\" && /workspace/build/debug/bin/data_server --memory_limit_bytes=$SIMM_MEMORY_LIMIT && tail -f /dev/null"]
          {{ else }}
          command: ["/bin/bash", "-c", "sysctl -w kernel.core_pattern=\"|/usr/bin/lz4 - /var/lib/systemd/coredump/core.%E.%h.lz4 -z -f\" && /workspace/build/release/bin/data_server --memory_limit_bytes=$SIMM_MEMORY_LIMIT"]
          {{ end }}
          volumeMounts:
            - name: shm
              mountPath: /dev/shm
            - name: coredump
              mountPath: /var/lib/systemd/coredump
            - name: sysfs
              mountPath: /sys
      volumes:
      - name: coredump
        hostPath:
          path: /var/lib/systemd/coredump
      - name: shm
        emptyDir:
          medium: Memory
      - name: sysfs
        hostPath:
          path: /sys
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: simm-disable
                operator: DoesNotExist
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                {{ if .Values.namespace }}
                - app-{{ .Values.namespace }}-simm-data-svc
                {{ else }}
                - app-simm-data-svc
                {{ end }}
            topologyKey: "kubernetes.io/hostname"
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
